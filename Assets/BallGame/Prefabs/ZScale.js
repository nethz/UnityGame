#pragma strict
/*
private var scale_:Vector3 = Vector3();
var zBegin_ = 460;
var zEnd_ = 660;
var offset_ = 0;
function Start () {
	this.scale_ = this.transform.localScale;
}

function Update () {
	var length = Mathf.Abs(MainCamera.getIt().transform.position.z - this.transform.position.z) - this.offset_;
	var scale = 1 -  (length -zBegin_)/(zEnd_ - zBegin_) *0.5;
	this.transform.localScale.x = scale * scale_.x;
	this.transform.localScale.y = scale * scale_.y;
	this.transform.localScale.z = scale * scale_.z;
} */